name: little_vegan_eats
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^2.0.10
  dio: ^5.0.0
  html: ^0.15.6
  firebase_core: ^3.15.2
  firebase_auth: ^5.2.0
  cupertino_icons: ^1.0.8
  cloud_firestore: ^5.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/icons/